# v2.5.2 发布验收

基线 v2.5.1 / e735dba92ebc636fb98e5d861177c49c444a2766。本版实现独立 SubAgent 用量采集，保留三层路由与原有全部边界。

本地 Python 3.13.5：完整 204 项测试通过（原有 153 项 + 51 项 token 统计测试）。RoutePlan 示例和 install.sh Bash 语法检查通过。全量安装/重复安装保留 routing、outcome、usage 和用户 hooks 文件；新脚本完整刷新。根 SKILL.md 为 5666 bytes，原 <6000 / profile <950 预算不变。

合成数据覆盖：SI 数值进位、四项明细与子项口径、原始整数和 null、child/parent 边界、ordinal、继承 prefix、缺失基线、累计重复、计数重置/缺口、未刷盘、模型变化、App Server 零基线、只读受限路径、幂等/并发、receipt 显式关联与 finalize、独立 on/off、project 非 Git 配置、hook 错误不触发继续执行、保留自定义配置和异常回滚。

CI 延续 Linux/Python 3.12 和 3.13、macOS/Python 3.12、Windows/Python 3.12。Windows 跳过已有 POSIX Bash 安装项；Python 助手仍测试。实际远端最终状态以 PR checks 为准，本文不提前宣称 CI 通过。

真实 Codex Desktop 支持、hooks 信任入口、自然事件触发、当地日志格式与完整覆盖尚需用户环境验收；本地 stdout 模拟和 CI 并不替代。停止阶段日志可能未写入终止标记，应先 partial 后在 finalize 复核；完整快照不等于后台账单结算。
