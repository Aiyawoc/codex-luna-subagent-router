# Agent Router v2.7.0 — Stable Acceptance

Date: 2026-09-23

## Release basis

- Candidate validated: `v2.7.0-rc.4`
- RC.4 commit: `a92d1a57573e29c09d4bd590ad638dd1cfcd6c12`
- Environment: macOS 26.5.2, arm64
- Router: `2.7.0-rc.4`
- Routing mode: `luna_only`
- Decision Engine: off
- Desktop Host version was not independently established by the final acceptance run; the visible Codex CLI was 0.154.0 and was not used as a Desktop-version proxy.

## Installation gate

Final RC.4 acceptance verified:

- release-package SHA256;
- bundled `doctor --verify` before installation;
- global install/upgrade;
- `doctor --verify` after installation;
- target VERSION;
- preservation of routing, hooks, AGENTS.md unmanaged content, unrelated profiles, and existing ledgers.

## Real Host A → B → C gate

### Turn A

A fresh `gpt-6-luna/high` Worker W was created and completed.

- baseline: `fresh`
- exact end boundary: known
- child snapshot: complete
- frozen total interval: 102,095 tokens

### Turn B

A new real parent turn in the same Host session naturally reused the exact same Worker W.

- RoutePlan: `delegate / subagent`
- minimum capability: `luna`
- no new SubagentStart
- MultiAgentV2 Interacted observed
- Completed observed
- baseline: `exact_cursor`
- Worker lifetime before: 102,095
- Worker lifetime after: 138,501

### Turn C

A third real user turn performed read-only post-Stop verification.

- Turn B phase: `sealed`
- Turn B child turn identity: observed
- exact end boundary: known
- child snapshot: present and complete
- child snapshot model/effort: `gpt-6-luna/high`
- Turn B interval: 36,406 tokens
- 138,501 − 102,095 = 36,406, consistent with the exact cursor/end-boundary interval
- Worker lifetime was not substituted for the per-turn interval
- `conflicting_session_headers`: not observed
- unknown values were not converted to zero

Turn A remained unchanged after Turn B/C: its interval, boundary, and child identity stayed frozen, with no cross-turn contamination.

## Receipt note

The final Turn B did not create an Outcome receipt, so receipt/finalize comparison was not available in this particular Host run. This was non-blocking under the frozen acceptance criteria; receipt interval behavior is covered by the RC.2 fix and regression suite.

## Stable decision

**PASS — READY FOR v2.7.0**

The stable publication workflow must still verify the final stable commit itself: complete Manifest, full tests, Router Arena, and all four portable package build/smoke targets before creating the immutable `v2.7.0` release.
