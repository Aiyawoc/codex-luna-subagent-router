# v2.5.1 发布验收记录

基线：main v2.5.0 `721af18528ec211b46c70b77cf5e9892e52f9571`；继续已有 `v2.5.1-outcome-collection` 设计分支。

本轮将审查覆盖层合入完整源码，保留既有配置脚本、profiles、测试与发布流程。初次完整回归发现渐进披露文档契约缺失，已修复文档，没有删除或放宽原测试。

补充验证：批量分组不能收缩成依赖循环；Lead 保留任务与运行中 Worker 的读写所有权参与冲突检查；in_progress_task_ids 不重复派遣；用户/项目 Codex 和 routing.json 的更小容量均生效；begin/finalize 共用互斥锁；无关 receipt 字段和不合法 observed metadata 不被展示/计入校准。

## 本地自动验收

- Python 3.13.5：完整 153 项单元/集成测试通过。
- RoutePlan 示例校验通过；install.sh Bash 语法检查通过。
- 全量安装与重复安装测试通过：新脚本完整复制、旧托管 Terra 清理、自定义 profile 与 routing/outcome 数据保留。
- 原 97 项回归、审查包 44 项新增、发布整合 12 项新增均纳入本次测试。
- 指令预算仍使用原约束：根 Skill < 6000 bytes、随包 Worker profiles < 950 bytes。

CI 扩展到 Ubuntu/Python 3.12、Ubuntu/Python 3.13、macOS/Python 3.12、Windows/Python 3.12；POSIX 安装测试在 Windows 明确跳过，Python 助手仍测试。CI 检查 Manifest 覆盖所有 Git tracked 文件且逐字节一致；新增 LF 属性避免平台换行差异。远端最终状态以本 PR 的实际 checks 为准，本文件不提前宣称远端通过。

## 实机验收仍待用户环境

自动测试不是自然派遣率、运行模型身份或费用收益的证明。升级后从项目目录测试：一次双任务整组规划；一次 begin/finalize 入库；一次未知身份 partial；stats pending 为零（或说明未结算原因）；旧 global 历史仍可只读查看。没有新增引擎 hook，未 begin 的 Worker 无法被收集器发现。
