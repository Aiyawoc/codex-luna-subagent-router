interface:
  display_name: "Luna SubAgent 自动路由"
  short_description: "按难度调度 Luna 中/高/极高/最高 Worker，并隔离旧任务上下文"
  default_prompt: "使用 $codex-luna-subagent-router 判断是否值得委派；派遣前披露计划，随后用新线程和本轮任务包创建 Luna Worker。"
policy:
  allow_implicit_invocation: true
